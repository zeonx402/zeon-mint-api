{
  "type": "object",
  "required": ["p", "op", "tick", "id", "amt"],
  "properties": {
    "p": { "const": "x402", "description": "Protocol" },
    "op": { "const": "mint", "description": "Operation" },
    "tick": { "const": "zeon", "description": "Token Ticker" },
    "id": {
      "type": "string",
      "title": "Mint ID",
      "description": "معرّف العملية (حرّر أو اتركه تلقائيًا)"
    },
    "amt": {
      "type": "string",
      "title": "Amount (ZEON)",
      "pattern": "^[0-9]+$",
      "default": "1250",
      "description": "كل 1250 ZEON = 1.5 USDC"
    }
  }
}
